今天其实就是，想要用IIC实现，stm32和EEPROM的写入和读取

1. 首先我们还是要满足IIC写入和读取的过程，
2. 然后就是你要满足EEPROM的读写时序，这里有一个要点就是你想要读取的时候，你先输入W信号之后，就要把SDA的操控权给从设备，你就不知道要具体读的地址，所以要有一个`假写真读`，先把要读的地址写进去
3. 今天是用软件模拟的方式，所以它使用的GPIO，因为要使用总线，所以要设置成开漏输出模式，
4. 而读写，说白了就是对外输出和输入这两种
   - 写，输出：就是让对应的ODR寄存器输出0/1  ，直接或/与非对应位的宏就好了
   - 度，输入： 这里的是要看IDR寄存器，要直接&

